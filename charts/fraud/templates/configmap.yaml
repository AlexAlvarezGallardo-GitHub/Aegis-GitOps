apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "fraud.fullname" . }}-config
  labels:
    {{- include "fraud.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}

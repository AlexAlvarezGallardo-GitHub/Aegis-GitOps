apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Placeholder for the observability stack (Sprint 5).
# Will deploy Prometheus, Grafana, Alertmanager, Loki, Tempo.
namespace: monitoring
resources:
  - namespace.yaml
  - placeholder.yaml

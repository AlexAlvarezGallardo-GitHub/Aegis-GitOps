apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Placeholder for the logging stack (Sprint 5).
# Will deploy Loki, Promtail and log collectors.
namespace: logging
resources:
  - namespace.yaml
  - placeholder.yaml

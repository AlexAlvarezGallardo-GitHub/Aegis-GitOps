apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres-audit
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres-audit
  template:
    metadata:
      labels:
        app: postgres-audit  
    spec:
      containers:
        - name: postgres
          image: postgres:16.4-alpine
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_USER
              value: aegis
            - name: POSTGRES_PASSWORD
              value: aegis_secret
            - name: POSTGRES_DB
              value: aegis_audit
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Base layer of Aegis-GitOps. Holds shared reference values consumed by the environment overlays.
configMapGenerator:
  - name: aegis-base-values
    files:
      - values.yaml